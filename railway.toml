[build]
builder = "nixpacks"

[build.nixpacksPlan]
providers = ["python"]

[deploy]
startCommand = "pip install -r backend/requirements.txt && cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
